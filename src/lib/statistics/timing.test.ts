import { describe, it, expect } from 'vitest';
import { getAccessTime } from './timing';
import { calculateMetrics } from './metrics';

describe('Statistics and Timing', () => {
    it.todo('calculates hit time correctly');
    it.todo('calculates load-through miss time correctly');
    it.todo('calculates non-load-through miss time correctly');
    it.todo('calculates metrics correctly');
    it.todo('handles zero accesses in metrics without NaN');
});
