import type { CacheConfig, ComparisonResult } from '../cache/types';
import { CacheSimulator } from './CacheSimulator';

export function compareCaches(config: CacheConfig, sequence: number[]): ComparisonResult {
    // TODO: Run simulation for both Direct-Mapped and Fully Associative MRU caches
    // using the exact same sequence.
    
    // Return dummy data for now
    return {} as ComparisonResult;
}
