import type { SimulationResult } from '../cache/types';

export function formatTraceToText(result: SimulationResult): string {
    // TODO: Convert a complete TraceEntry array into a downloadable plain text format
    return "";
}
